import React from 'react';
import {HeaderDiv, HeaderTitle} from '../styles/Header.styled';

function Header() {
    return (
        <HeaderDiv>
            <HeaderTitle>React Redux Notes</HeaderTitle>
        </HeaderDiv>
    )
}

export default Header
